schema_version = "1.0"

id = "no-panic-production-burndown"
title = "No-panic production debt burndown"
status = "active"
owner = "EffortlessMetrics"
created = "2026-08-04"

objective = """
Burn down production-path panic-family debt in bounded, review-forward PRs
without resetting the baseline or weakening the no-new-debt policy. Each slice
must preserve the fixture library's deterministic, usable public behavior while
making failure behavior explicit at the narrowest responsible boundary.
"""

end_state = [
  "The no-panic checker reports no unreceipted new production debt.",
  "Each repaired production seam has focused regression proof and a bounded claim boundary.",
  "Historical baseline debt remains visible and is not silently absorbed into a reset.",
  "Remaining compatibility, release, and provider claims stay outside this lane unless separately specified.",
]

[[work_item]]
id = "pgp-production-no-panic"
status = "done"
proposal = "USELESSKEY-PROP-0001"
spec = "USELESSKEY-SPEC-0005"
plan = "plans/no-panic-burndown/implementation-plan.md"
commands = [
  "cargo xtask check-no-panic-family",
  "cargo test -p uselesskey-pgp",
  "cargo xtask pr",
  "git diff --check",
]

[[work_item]]
id = "pkcs11-mock-test-no-panic"
status = "done"
proposal = "USELESSKEY-PROP-0001"
spec = "USELESSKEY-SPEC-0005"
plan = "plans/no-panic-burndown/implementation-plan.md"
commands = [
  "cargo xtask check-no-panic-family",
  "cargo test -p uselesskey-pkcs11-mock",
  "cargo xtask pr",
  "git diff --check",
]

[[work_item]]
id = "ecdsa-validation-example-no-panic"
status = "done"
proposal = "USELESSKEY-PROP-0001"
spec = "USELESSKEY-SPEC-0005"
plan = "plans/no-panic-burndown/implementation-plan.md"
commands = [
  "cargo xtask check-no-panic-family",
  "cargo test --manifest-path examples/external/ecdsa-fixture-validation/Cargo.toml",
  "cargo xtask pr",
  "git diff --check",
]

[[work_item]]
id = "ed25519-validation-example-no-panic"
status = "done"
proposal = "USELESSKEY-PROP-0001"
spec = "USELESSKEY-SPEC-0005"
plan = "plans/no-panic-burndown/implementation-plan.md"
commands = [
  "cargo xtask check-no-panic-family",
  "cargo test --manifest-path examples/external/ed25519-fixture-validation/Cargo.toml",
  "cargo xtask pr",
  "git diff --check",
]

[[work_item]]
id = "oidc-validation-example-no-panic"
status = "done"
proposal = "USELESSKEY-PROP-0001"
spec = "USELESSKEY-SPEC-0005"
plan = "plans/no-panic-burndown/implementation-plan.md"
commands = [
  "cargo xtask check-no-panic-family",
  "cargo test --manifest-path examples/external/oidc-test-server-validation/Cargo.toml",
  "cargo xtask pr",
  "git diff --check",
]

[[work_item]]
id = "pgp-validation-example-no-panic"
status = "done"
proposal = "USELESSKEY-PROP-0001"
spec = "USELESSKEY-SPEC-0005"
plan = "plans/no-panic-burndown/implementation-plan.md"
commands = [
  "cargo xtask check-no-panic-family",
  "cargo test --manifest-path examples/external/pgp-fixture-validation/Cargo.toml",
  "cargo clippy --manifest-path examples/external/pgp-fixture-validation/Cargo.toml --all-targets -- -D warnings",
  "cargo xtask pr",
  "git diff --check",
]

[[work_item]]
id = "pkcs11-validation-example-no-panic"
status = "done"
proposal = "USELESSKEY-PROP-0001"
spec = "USELESSKEY-SPEC-0005"
plan = "plans/no-panic-burndown/implementation-plan.md"
commands = [
  "cargo xtask check-no-panic-family",
  "cargo test --manifest-path examples/external/pkcs11-mock-validation/Cargo.toml",
  "cargo clippy --manifest-path examples/external/pkcs11-mock-validation/Cargo.toml --all-targets -- -D warnings",
  "cargo xtask pr",
  "git diff --check",
]

[[work_item]]
id = "ssh-validation-example-no-panic"
status = "done"
proposal = "USELESSKEY-PROP-0001"
spec = "USELESSKEY-SPEC-0005"
plan = "plans/no-panic-burndown/implementation-plan.md"
commands = [
  "cargo xtask check-no-panic-family",
  "cargo test --manifest-path examples/external/ssh-fixture-validation/Cargo.toml",
  "cargo clippy --manifest-path examples/external/ssh-fixture-validation/Cargo.toml --all-targets -- -D warnings",
  "cargo xtask pr",
  "git diff --check",
]

[[work_item]]
id = "webauthn-validation-example-no-panic"
status = "done"
proposal = "USELESSKEY-PROP-0001"
spec = "USELESSKEY-SPEC-0005"
plan = "plans/no-panic-burndown/implementation-plan.md"
commands = [
  "cargo xtask check-no-panic-family",
  "cargo test --manifest-path examples/external/webauthn-ceremony-validation/Cargo.toml",
  "cargo clippy --manifest-path examples/external/webauthn-ceremony-validation/Cargo.toml --all-targets -- -D warnings",
  "cargo xtask pr",
  "git diff --check",
]

[[work_item]]
id = "docs-sync-adapter-test-no-panic"
status = "done"
proposal = "USELESSKEY-PROP-0001"
spec = "USELESSKEY-SPEC-0005"
plan = "plans/no-panic-burndown/implementation-plan.md"
commands = [
  "cargo xtask check-no-panic-family",
  "cargo test -p xtask docs_sync",
  "cargo clippy -p xtask --all-targets -- -D warnings",
  "cargo xtask pr",
  "git diff --check",
]

[[work_item]]
id = "external-adoption-readme-tests-no-panic"
status = "done"
proposal = "USELESSKEY-PROP-0001"
spec = "USELESSKEY-SPEC-0005"
plan = "plans/no-panic-burndown/implementation-plan.md"
commands = [
  "cargo xtask check-no-panic-family",
  "cargo test -p xtask external_adoption_examples_readme",
  "cargo clippy -p xtask --all-targets -- -D warnings",
  "cargo xtask pr",
  "git diff --check",
]

[[work_item]]
id = "external-adoption-setup-tests-no-panic"
status = "done"
proposal = "USELESSKEY-PROP-0001"
spec = "USELESSKEY-SPEC-0005"
plan = "plans/no-panic-burndown/implementation-plan.md"
commands = [
  "cargo xtask check-no-panic-family",
  "cargo test -p xtask external_adoption_smoke external_adoption_ci_recipes",
  "cargo clippy -p xtask --all-targets -- -D warnings",
  "cargo xtask pr",
  "git diff --check",
]

[[work_item]]
id = "external-adoption-receipt-rendering-no-panic"
status = "done"
proposal = "USELESSKEY-PROP-0001"
spec = "USELESSKEY-SPEC-0005"
plan = "plans/no-panic-burndown/implementation-plan.md"
commands = [
  "cargo xtask check-no-panic-family",
  "cargo test -p xtask external_adoption_markdown_lists_cli_loop_and_boundaries",
  "cargo clippy -p xtask --all-targets -- -D warnings",
  "cargo xtask pr",
  "git diff --check",
]

[[work_item]]
id = "external-adoption-actions-matrix-no-panic"
status = "ready"
proposal = "USELESSKEY-PROP-0001"
spec = "USELESSKEY-SPEC-0005"
plan = "plans/no-panic-burndown/implementation-plan.md"
commands = [
  "cargo xtask check-no-panic-family",
  "cargo test -p xtask external_adoption_ci_recipe_profiles_match_actions_matrix",
  "cargo clippy -p xtask --all-targets -- -D warnings",
  "cargo xtask pr",
  "git diff --check",
]
