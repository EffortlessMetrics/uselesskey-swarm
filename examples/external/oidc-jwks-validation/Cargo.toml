[package]
name = "uselesskey-external-oidc-jwks-validation"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
uselesskey = { version = "0.9.1", default-features = false, features = ["rsa", "jwk"] }

[workspace]
