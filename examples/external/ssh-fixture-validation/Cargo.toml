[package]
name = "uselesskey-external-ssh-fixture-validation"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
ssh-key = { version = "0.6.7", default-features = false, features = ["std", "ed25519", "rsa"] }
uselesskey-core = { version = "0.9.1", default-features = false }
uselesskey-ssh = "0.9.1"

[workspace]
