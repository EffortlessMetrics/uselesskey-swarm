[package]
name = "uselesskey-external-entropy-byte-fixtures"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
uselesskey-core = { version = "0.9.1", default-features = false }
uselesskey-entropy = "0.9.1"

[workspace]
