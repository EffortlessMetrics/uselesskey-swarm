# Rails Specs

Specs own behavior and proof contracts.

Use this directory for Rails-scoped specs that define portable framework
behavior. Existing uselesskey specs under `docs/specs/` remain valid and still
feed the current spec checker.

