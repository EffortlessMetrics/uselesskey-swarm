# Rails Proposals

Proposals own why work exists.

Use this directory for Rails-scoped proposals that should be portable across
repos. Existing uselesskey proposals under `docs/proposals/` remain valid until
a migration lane moves or mirrors them.

