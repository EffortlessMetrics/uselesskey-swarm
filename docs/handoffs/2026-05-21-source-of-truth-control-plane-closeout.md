+++
id = "USELESSKEY-HANDOFF-2026-05-21-source-of-truth-control-plane"
kind = "closeout"
title = "Source-of-truth control plane closeout"
status = "implemented"
owner = "codex"
created = "2026-05-21"
linked_proposal = "USELESSKEY-PROP-0001"
linked_specs = [
  "USELESSKEY-SPEC-0002",
  "USELESSKEY-SPEC-0006",
  "USELESSKEY-SPEC-0011",
  "USELESSKEY-SPEC-0016",
  "USELESSKEY-SPEC-0017",
  "USELESSKEY-SPEC-0020",
  "USELESSKEY-SPEC-0022",
  "USELESSKEY-SPEC-0023",
]
linked_adrs = [
  "USELESSKEY-ADR-0001",
  "USELESSKEY-ADR-0002",
  "USELESSKEY-ADR-0003",
  "USELESSKEY-ADR-0004",
]
linked_plan = "plans/source-of-truth-control-plane/implementation-plan.md"
support_tier_impact = [
  "docs/status/PUBLIC_CLAIMS.md",
  "docs/status/SUPPORT_TIERS.md",
]
policy_impact = [
  "policy/claim-ledger.toml",
  "policy/contract-packs.toml",
  "policy/doc-artifacts.toml",
  "policy/negative-fixtures.toml",
]
linked_prs = []
+++

# Source-of-truth Control Plane Closeout

Generated by `cargo xtask closeout --goal source-of-truth-control-plane`.

Active goal status at generation time: `active`.

## Landed Work

| Work item | Spec | Plan |
| --- | --- | --- |
| `doc-artifact-ledger` | `USELESSKEY-SPEC-0023` | `plans/source-of-truth-control-plane/implementation-plan.md` |
| `doc-artifact-checker` | `USELESSKEY-SPEC-0023` | `plans/source-of-truth-control-plane/implementation-plan.md` |
| `claim-ledger-payload` | `USELESSKEY-SPEC-0002` | `plans/source-of-truth-control-plane/implementation-plan.md` |
| `support-tier-map` | `USELESSKEY-SPEC-0002` | `plans/source-of-truth-control-plane/implementation-plan.md` |
| `support-tier-checker` | `USELESSKEY-SPEC-0023` | `plans/source-of-truth-control-plane/implementation-plan.md` |
| `control-plane-implementation-plan` | `USELESSKEY-SPEC-0023` | `plans/source-of-truth-control-plane/implementation-plan.md` |
| `active-goal-manifest` | `USELESSKEY-SPEC-0023` | `plans/source-of-truth-control-plane/implementation-plan.md` |
| `goal-manifest-checker` | `USELESSKEY-SPEC-0023` | `plans/source-of-truth-control-plane/implementation-plan.md` |
| `agent-operating-contract` | `USELESSKEY-SPEC-0023` | `plans/source-of-truth-control-plane/implementation-plan.md` |
| `github-pr-and-issue-templates` | `USELESSKEY-SPEC-0023` | `plans/source-of-truth-control-plane/implementation-plan.md` |
| `advisory-source-of-truth-ci` | `USELESSKEY-SPEC-0023` | `plans/source-of-truth-control-plane/implementation-plan.md` |
| `repo-contract-report` | `USELESSKEY-SPEC-0023` | `plans/source-of-truth-control-plane/implementation-plan.md` |
| `pr-body-generator` | `USELESSKEY-SPEC-0023` | `plans/source-of-truth-control-plane/implementation-plan.md` |
| `closeout-generator` | `USELESSKEY-SPEC-0023` | `plans/source-of-truth-control-plane/implementation-plan.md` |
| `oidc-jwks-contract-pack` | `USELESSKEY-SPEC-0017` | `plans/source-of-truth-control-plane/implementation-plan.md` |
| `jwt-token-negative-contract` | `USELESSKEY-SPEC-0016` | `plans/source-of-truth-control-plane/implementation-plan.md` |
| `webhook-contract-pack` | `USELESSKEY-SPEC-0011` | `plans/source-of-truth-control-plane/implementation-plan.md` |
| `tls-x509-contract-pack` | `USELESSKEY-SPEC-0016` | `plans/source-of-truth-control-plane/implementation-plan.md` |
| `first-five-minutes-path` | `USELESSKEY-SPEC-0022` | `plans/source-of-truth-control-plane/implementation-plan.md` |
| `downstream-ci-recipes` | `USELESSKEY-SPEC-0020` | `plans/source-of-truth-control-plane/implementation-plan.md` |
| `release-handoff-packet` | `USELESSKEY-SPEC-0006` | `plans/source-of-truth-control-plane/implementation-plan.md` |

## Required Evidence

```bash
cargo clippy -p xtask --all-targets -- -D warnings
cargo fmt --check -p xtask
cargo test -p uselesskey-cli --all-features bundle
cargo test -p uselesskey-cli --all-features tls
cargo test -p uselesskey-cli --all-features webhook
cargo test -p uselesskey-jwk --all-features
cargo test -p uselesskey-token --all-features
cargo test -p uselesskey-x509 --all-features
cargo test -p xtask closeout
cargo test -p xtask doc_artifacts
cargo test -p xtask goals
cargo test -p xtask pr_body
cargo test -p xtask repo_contract_report
cargo test -p xtask support_tiers
cargo xtask check-doc-artifacts
cargo xtask check-goals
cargo xtask check-negative-fixtures
cargo xtask check-support-tiers
cargo xtask claim-report --check-public-claims
cargo xtask docs-sync --check
cargo xtask external-adoption-smoke --path .
cargo xtask external-adoption-smoke --path . --library-examples
cargo xtask no-blob
cargo xtask pr
cargo xtask pr-body --work-item goal-manifest-checker
cargo xtask repo-contract-report
cargo xtask spec-check --strict
cargo xtask typos
git diff --check
python -c "import pathlib,tomllib; tomllib.loads(pathlib.Path('.uselesskey/goals/active.toml').read_text()); print('active goal parses')"
python -c "import pathlib,tomllib; tomllib.loads(pathlib.Path('policy/claim-ledger.toml').read_text()); print('claim ledger parses')"
python -c "import pathlib,tomllib; tomllib.loads(pathlib.Path('policy/doc-artifacts.toml').read_text()); print('doc artifact ledger parses')"
python -c "import yaml, pathlib; yaml.safe_load(pathlib.Path('.github/workflows/source-of-truth.yml').read_text()); print('source-of-truth workflow parses')"
```

## Receipts

- `.uselesskey/goals/archive/2026-05-21-source-of-truth-control-plane.toml`
- `docs/handoffs/2026-05-21-source-of-truth-control-plane-closeout.md`

## Support-Tier Impact

- `docs/status/PUBLIC_CLAIMS.md`
- `docs/status/SUPPORT_TIERS.md`

Related claims:
- `scanner-safe-fixtures` (`stable`) via `USELESSKEY-SPEC-0002` - Scanner-safe fixtures; surfaces: `README badge`; `docs/VERIFICATION.md`; `docs/reference/audit-surface.md`; `docs/status/PUBLIC_CLAIMS.md`; proof: `cargo xtask scanner-safe-reference --check`; `cargo xtask no-blob`; `cargo xtask badges --check`
- `ripr-plus-evidence-endpoint` (`stable`) via `USELESSKEY-SPEC-0002` - ripr+ evidence endpoint; surfaces: `README badge`; `badges/ripr-plus.json`; `docs/VERIFICATION.md`; `docs/status/PUBLIC_CLAIMS.md`; proof: `cargo xtask badges --check`; `cargo xtask test-efficiency-report`
- `tls-contract-pack` (`stable`) via `USELESSKEY-SPEC-0016` - TLS contract pack; surfaces: `README`; `docs/how-to/test-tls-chain-validation.md`; `docs/status/PUBLIC_CLAIMS.md`; proof: `cargo xtask bundle-proof --profile tls --out target/release-evidence/tls`; `cargo xtask no-blob`
- `oidc-jwks-contract-pack` (`stable`) via `USELESSKEY-SPEC-0017` - OIDC/JWKS contract pack; surfaces: `README`; `docs/how-to/test-oidc-jwks-validation.md`; `docs/status/PUBLIC_CLAIMS.md`; proof: `cargo xtask bundle-proof --profile oidc --out target/release-evidence/oidc`; `cargo xtask no-blob`
- `jwt-token-negative-fixtures` (`advisory`) via `USELESSKEY-SPEC-0016` - JWT/token negative fixtures; surfaces: `docs/how-to/test-jwt-negative-validation.md`; `docs/status/negative-fixture-matrix.md`; `policy/negative-fixtures.toml`; `docs/status/PUBLIC_CLAIMS.md`; proof: `cargo test -p uselesskey-token --all-features`; `cargo xtask check-negative-fixtures`
- `webhook-contract-pack` (`stable`) via `USELESSKEY-SPEC-0011` - Webhook contract pack; surfaces: `docs/how-to/test-webhook-signature-validation.md`; `docs/status/PUBLIC_CLAIMS.md`; `policy/contract-packs.toml`; proof: `cargo xtask bundle-proof --profile webhook --out target/release-evidence/webhook`; `cargo xtask no-blob`
- `bundle-manifest-schema` (`advisory`) via `USELESSKEY-SPEC-0017` - Bundle manifest schema; surfaces: `docs/schemas/bundle-manifest.schema.json`; `uselesskey bundle`; `uselesskey verify-bundle`; `docs/status/PUBLIC_CLAIMS.md`; proof: `cargo xtask check-bundle-schemas`
- `negative-coverage-receipt` (`advisory`) via `USELESSKEY-SPEC-0016` - Negative coverage receipt; surfaces: `docs/schemas/negative-coverage.schema.json`; `receipts/negative-coverage.json`; `docs/status/negative-fixture-matrix.md`; `docs/status/PUBLIC_CLAIMS.md`; proof: `cargo xtask check-negative-fixtures`; `cargo xtask check-bundle-schemas`
- `public-crate-surface-cleanup` (`stable`) via `USELESSKEY-SPEC-0002` - Public crate-surface cleanup; surfaces: `README workspace crate list`; `docs/architecture/public-surface.md`; `docs/how-to/migrate-to-v0.8.md`; `docs/status/PUBLIC_CLAIMS.md`; proof: `cargo xtask public-surface`; `cargo xtask publish-check`; `cargo xtask publish-preflight`
- `external-cratesio-install-smoke` (`release-proof`) via `USELESSKEY-SPEC-0002` - External crates.io install smoke; surfaces: `release evidence`; `docs/release/post-release-audit.md`; `docs/status/PUBLIC_CLAIMS.md`; proof: `cargo xtask cratesio-smoke --version 0.9.1`
- `generated-badge-endpoints` (`stable`) via `USELESSKEY-SPEC-0002` - Generated badge endpoints; surfaces: `README badge masthead`; `badges/README.md`; `docs/VERIFICATION.md`; `docs/status/PUBLIC_CLAIMS.md`; proof: `cargo xtask badges`; `cargo xtask badges --check`
- `ripr-pr-review-evidence` (`advisory`) via `USELESSKEY-SPEC-0002` - ripr PR review evidence; surfaces: `PR summaries`; `GitHub warning annotations`; `CI artifacts`; `docs/VERIFICATION.md`; `docs/status/PUBLIC_CLAIMS.md`; proof: `cargo xtask ripr-pr --check`; `cargo xtask ripr-review-comments --check`; `cargo xtask ripr-pr-summary --check`

## Policy Impact

- `policy/claim-ledger.toml`
- `policy/contract-packs.toml`
- `policy/doc-artifacts.toml`
- `policy/negative-fixtures.toml`

## Claim Boundary

The webhook contract pack proves deterministic HMAC webhook verifier behavior
for generated fixture requests.

It does not prove production webhook provider compatibility, secret rotation,
delivery retries, timestamp policy suitability, replay protection completeness,
transport security, production secret management, or downstream verifier
correctness.

Generated request packs are test artifacts. Users should keep generated
secret-shaped payloads under `target/` or another ignored build directory and
share receipts or verification packs instead of generated payload blobs.

## Remaining Work

None recorded in this active goal.

## Known Risks

- Generated closeouts are summaries of repo manifests and ledgers; they do not replace hosted CI evidence.
- Archive copies preserve the active goal at generation time; follow-up work must choose or update the next active goal explicitly.
- Release, publish, signing, tags, GitHub releases, and public source sync remain outside the swarm boundary.

## Rollback

Rollback should be per checker or per artifact family:

- revert a checker PR if it produces false positives;
- keep advisory CI non-blocking during burn-in;
- mark artifacts `superseded` instead of deleting stable IDs already referenced
  by docs, ledgers, receipts, or PRs;
- revert generated PR-body or closeout tooling without changing release
  authority.

## Next Safe Action

Create or select the next active goal before starting new work.
