+++
id = "USELESSKEY-HANDOFF-2026-05-21-v0-10-0-release-readiness"
kind = "closeout"
title = "v0.10.0 release readiness closeout"
status = "implemented"
owner = "codex"
created = "2026-05-21"
linked_proposal = "USELESSKEY-PROP-0001"
linked_specs = [
  "USELESSKEY-SPEC-0024",
]
linked_adrs = [
  "USELESSKEY-ADR-0002",
  "USELESSKEY-ADR-0003",
  "USELESSKEY-ADR-0004",
]
linked_plan = "plans/v0.10.0-release-readiness/implementation-plan.md"
support_tier_impact = []
policy_impact = []
linked_prs = []
+++

# V0.10.0 Release Readiness Closeout

Generated by `cargo xtask closeout --goal v0.10.0-release-readiness`.

Active goal status at generation time: `active`.

## Landed Work

| Work item | Spec | Plan |
| --- | --- | --- |
| `open-release-readiness-goal` | `USELESSKEY-SPEC-0024` | `plans/v0.10.0-release-readiness/implementation-plan.md` |
| `release-surface-inventory` | `USELESSKEY-SPEC-0024` | `plans/v0.10.0-release-readiness/implementation-plan.md` |
| `version-snippet-reconciliation` | `USELESSKEY-SPEC-0024` | `plans/v0.10.0-release-readiness/implementation-plan.md` |
| `installed-cli-release-smoke` | `USELESSKEY-SPEC-0024` | `plans/v0.10.0-release-readiness/implementation-plan.md` |
| `facade-release-smoke` | `USELESSKEY-SPEC-0024` | `plans/v0.10.0-release-readiness/implementation-plan.md` |
| `package-dry-run` | `USELESSKEY-SPEC-0024` | `plans/v0.10.0-release-readiness/implementation-plan.md` |
| `release-readiness-record` | `USELESSKEY-SPEC-0024` | `plans/v0.10.0-release-readiness/implementation-plan.md` |

## Required Evidence

```bash
cargo test -p uselesskey-cli --all-features audit_bundle
cargo test -p uselesskey-ecdsa --all-features
cargo test -p uselesskey-ed25519 --all-features
cargo test -p uselesskey-rsa --all-features
cargo test -p xtask external_adoption
cargo xtask check-doc-artifacts
cargo xtask check-goals
cargo xtask check-support-tiers
cargo xtask docs-sync --check
cargo xtask external-adoption-smoke --path .
cargo xtask external-adoption-smoke --path . --library-examples
cargo xtask lint-fix --check --no-clippy
cargo xtask no-blob
cargo xtask pr
cargo xtask publish-check
cargo xtask publish-preflight
cargo xtask typos
git diff --check
```

## Receipts

- `.uselesskey/goals/archive/2026-05-21-v0-10-0-release-readiness.toml`
- `docs/handoffs/2026-05-21-v0-10-0-release-readiness-closeout.md`

## Support-Tier Impact

None recorded.

## Policy Impact

None recorded.

## Claim Boundary

This lane proves release readiness for documented test-fixture workflows. It
does not prove production security, provider compatibility, downstream verifier
correctness, scanner-policy bypass approval, publish success, or future registry
state.

## Remaining Work

None recorded in this active goal.

## Known Risks

- Generated closeouts are summaries of repo manifests and ledgers; they do not replace hosted CI evidence.
- Archive copies preserve the active goal at generation time; follow-up work must choose or update the next active goal explicitly.
- Release, publish, signing, tags, GitHub releases, and public source sync remain outside the swarm boundary.

## Rollback

Revert the release-readiness PR and restore the prior active goal manifest if
the lane is opened prematurely. If a later readiness slice is wrong, revert that
slice and return its work item to `ready` or `blocked` with a concrete reason.

## Next Safe Action

Create or select the next active goal before starting new work.
